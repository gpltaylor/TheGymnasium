Update the Timetable module to use try dates from (from, to) this will allows searching to be
a little more interactive. However this does lend to this being a little more complicated.
moment("2015-08-23T06:00")

Fix bug where the Active menu item is not correct due to using a single module to serve multiple pages.
Need to include a custom property to state what the active crm page is.

Design needs to be made responsive and most elements at the moment don't work when pushed to mobile.
